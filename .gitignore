# Dependencies
node_modules/

# Build output
dist/
build/
cdk.out/
*.tsbuildinfo

# Environment
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Claude Code
.claude/
Snapshot_16.PNG

# Relations 元データ + デザイン資料 (JSONL だけで 2.4GB 超、design_handoff も外部資料)
FF display/

# Relations 生データの全バリアント (GitHub 100MB 制限超)
# scripts/normalize-relations.mjs --threshold=N で再生成可能。
# t500 (79MB) / depth2.json=t100 (169MB) / t10 (329MB) / t1 (693MB)
frontend/public/relations-app/data/depth2*.json

# scripts/normalize-relations.mjs を --output で誤指定して
# プロジェクトルートに書かれた生成物 (cwd 起点で出る場合あり)
/depth2.json
/depth2-*.json

# 別案件のデザインハンドオフ資料 (Git 管理対象外)
AVbuySite/
