---
title: Domain Traffic Inspector: ルール一覧のスクロール位置を保ち、タグの候補を出す
summary: 通信のたびに一覧が先頭へ戻るのを止め、タグ入力にサジェストを足した
projects: [domain-traffic-inspector]
---

ルールタブを見ている最中に通信が起きると、リストが描き直されてスクロール位置がリセットされていた。タグは毎回手で打つしかなかった。

![ルールタブ: スクロール位置の保持とタグの候補](/devlog/2026-04-07-domain-traffic-inspector-tag-suggest/tag-suggest.svg)

| 変更 | 中身 |
|---|---|
| スクロール位置の保持 | 通信によるリストの再描画でも、位置をリセットしない |
| タグのサジェスト | タグの入力に、既存のタグから候補を出すドロップダウンを足した |
| 一括タグ付け | prompt() での入力をやめ、モーダルダイアログにした |

変更は 2 ファイルで、282 行の追加と 46 行の削除。サイドパネルの中だけで完結する変更で、通信の集計やブロックの動作には手を入れていない。
